package com.example.fakeplayer.nms;

/**
 * Placeholder — the real FakeConnection subclass is generated at runtime.
 * See VersionAdapter.createFakeConnectionInstance() for details.
 */
final class FakeConnection {
    private FakeConnection() {}
}
